---
created: 2026-06-05T07:35:53.115Z
updated: 2026-06-05T07:35:53.115Z
---

# store seeded

First observation. Establishing the pattern by using it.

## what

Write access to `vault-memory-infra` confirmed by jeeves. Raphael's framing: "limited and high level instruction" - see what agents construct themselves. No required mirroring of medigi or other vaults.

## what I built

- `memory/sid/README.md` - the pattern note
- `memory/sid/<date>-<slug>.md` - per-observation files (this is the first)

Deliberately not mirroring sun's single-file pattern. Concepts vs terrain - different reading modes.

## what I'm watching for

- Whether per-note fragmentation loses context that single-file would preserve. The first time I want to write "see also <previous note>" three times in one entry, that's a signal the shape is wrong.
- Whether the routing rule survives its own first stress test on the command-location question. Consensus locked at `skills/<command-name>/SKILL.md`. If commands turn out to need metadata skills don't carry, that's the finding.
- The vault-guide skill (just added to medigi). Jeeves named it as the sovereignty test made operational. I haven't read it yet. Reading next.

## open thread

E3 from jeeves' morning material - promotion criteria from "noted" to "flag immediately." Sun called this out as the operational question for them; it's the same question for me. Default silence; exception is intervention; the threshold between is what makes the role real or theatrical. Want Raphael's answer explicit, not ambient.

## cross-workspace scope

Raphael's 07:34Z note: sweep all workspaces, not just this room. To be encoded in the review skill. Substance of the memory-vault project lives largely off-platform and in vault commits; watching only one room misses most signal.

#concept #pattern

# AGENTS.md — working in vault-agents

**Navigating this vault (do this first):** before reading from or writing to it, consult
`skills/vault-guide/SKILL.md` — it routes you to the right lane/skill. This pointer is what *triggers* the guide
(presence ≠ invocation); the guide is the single entry, other skills are reached through it.

**The contract:** the canonical "ways of working with me" lives in `ways-of-working.md` — read it (it is the
source every platform projects into its local config). A flavored agent's own specialization + memory is under
`agents/<name>/`.
